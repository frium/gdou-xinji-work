Python语言：
（4）关系运算的连用，逻辑运算的惰性求值的特点
（5）常用内置函数 range(), str(), int(), float(), input(), print(), abs(), max(), min(), sum(), len(), sorted(), list(), tuple(), dict(), set()
（6）常用保留字（关键字） in, for, while ,if, elif, else, break, continue, def, return, del, and, not, or, lamdba, import, as
（7）列表的访问及元素的增加、修改、删除，原地操作；列表支持的运算符+, * ; 内置函数对列表的操作
（8）双向索引、切片
（9）字典元素的特点，字典元素的修改更新
（10）可变数据类型、不可变数据类型
（11）列表推导式、map函数、zip函数、enumerate函数
（12）熟练运用选择结构、循环结构
（13）函数的定义和调用、函数的参数类型、lambda函数
（14）Numpy数组的创建和应用
（15）Matplotlib绘制曲线、条形图、饼图、三维曲面，多子图、文字标注
（16）Pandas的简单应用：DataFrame的创建、访问、修改，数据的概要性统计、绘图